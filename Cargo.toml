[package]
name = "dark_matter_crypto"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "dark_matter_crypto"
path = "src/main.rs"

[dependencies]
# No external dependencies - using only standard library for space efficiency
